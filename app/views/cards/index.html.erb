<h1>All Cards</h1>

<ul>
  <% @cards.each do |card| %>
    <li>
      <%= link_to card.card_name, card_path(card) %>
      <% if card.mana_cost.present? %> - <%= card.mana_cost %><% end %>
    </li>
  <% end %>
</ul>
